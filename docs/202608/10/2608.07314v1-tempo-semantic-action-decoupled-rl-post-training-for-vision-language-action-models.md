---
title: "TEMPO: Semantic-Action Decoupled RL Post-Training for Vision-Language-Action Models"
title_zh: TEMPO：视觉-语言-动作模型的语义-动作解耦强化学习后训练
authors: "Ziheng Liu, Quantao Yang"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07314v1"
tags: ["query:model"]
score: 9.0
evidence: 面向VLA模型的语义-动作解耦RL后训练
tldr: 视觉-语言-动作模型后训练常受分布不匹配困扰，现有RL对所有组件统一更新，忽略其功能差异。TEMPO冻结预训练骨干，解耦语义投影层与低层动作专家，以不同频率分别进行RL更新：投影层低速保持语义稳定，动作专家高速吸收交互反馈。在CALVIN基准和真实操作任务上，TEMPO超越预训练SOTA模型及RL后训练基线，并保持更高奖励。该解耦双时间尺度策略为VLA后训练提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.07314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 867, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1810, \"height\": 948, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1843, \"height\": 827, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.07314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 656, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07314v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07314v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 911, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07314v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 238, \"label\": \"Table\"}]"
motivation: 现有RL后训练对所有组件统一更新，忽略语义与动作模块的功能差异，导致策略更新可能破坏高维语义表示。
method: TEMPO冻结视觉-语言骨干，仅对语义投影层与动作专家分别进行低/高频RL更新，实现解耦优化。
result: 在CALVIN基准与真实操作任务上，TEMPO稳定超过预训练SOTA和RL后训练基线，奖励更高。
conclusion: 统一更新不可取，按模块职能分时标RL后训练既能保住语义表征，又能高效利用在线反馈。
---

## 摘要
视觉-语言-动作（VLA）模型通常通过监督微调（SFT）或在线强化学习（RL）后训练来适应下游操作任务。SFT容易产生分布不匹配问题，而现有的RL方法通常对所有模型组件应用单一、统一更新策略，忽略了它们不同的功能角色。我们提出TEMPO，一种针对VLA模型的语义-动作解耦、双时间尺度RL后训练框架。TEMPO冻结预训练的视觉-语言主干以保留通用语义表示，并将适应限制在具有专用RL优化循环的两个组件上：语义投影层和低级动作专家。我们以不同的速率更新它们——语义投影层不频繁更新，以保持潜在动作稳定；动作专家频繁更新，以快速整合来自在线交互的控制反馈。这种解耦的RL微调策略防止了快速策略更新破坏高层语义表示的稳定性，同时仍允许动作专家从在线反馈中高效学习。在CALVIN基准和真实世界操作任务上的实验表明，TEMPO始终优于预训练的最先进VLA模型和RL后训练基线，并在两个真实世界任务上达到并保持更高的评估奖励。

## Abstract
Vision-language-action (VLA) models are commonly adapted to downstream manipulation tasks via supervised fine-tuning (SFT) or online reinforcement learning (RL) post-training. SFT is prone to distribution mismatch, and existing RL approaches typically apply a single, uniform update strategy to all model components, ignoring their distinct functional roles. We propose TEMPO, a semantic-action decoupled, two-timescale RL post-training framework for VLA models. TEMPO freezes the pretrained vision-language backbone to preserve general semantic representations, and restricts adaptation to two components with dedicated RL optimization loops: the semantic projection layer and the low-level action expert. We update them at different rates--the semantic projection layer infrequently, to keep the latent action stable, and the action expert frequently, to rapidly incorporate control feedback from online interaction. This decoupling RL fine-tuning strategy prevents fast policy updates from destabilizing high-level semantic representations while still allowing the action expert to learn efficiently from online feedback. Experiments on the CALVIN benchmark and real-world manipulation tasks demonstrate that TEMPO consistently outperforms both pretrained state-of-the-art VLA models and the RL post-training baseline, while reaching and maintaining higher evaluation rewards on two real-world tasks.

---

## 论文详细总结（自动生成）

# TEMPO 论文总结

## 1. 核心问题与整体含义
- **研究动机**：视觉-语言-动作（VLA）模型在适应下游操作任务时，常用监督微调（SFT）或在线强化学习（RL）后训练。SFT 容易产生分布不匹配问题；而现有 RL 后训练方法往往对所有模型组件采用统一、单一的学习率或更新策略，忽略了视觉编码器、语言模型、语义投影层、动作专家等组件的不同功能角色。
- **核心问题**：快速统一的策略更新可能破坏 VLA 模型中高维、稳定的语义表示，导致策略学习不稳定或泛化能力下降。
- **整体含义**：论文提出一种解耦的 RL 后训练范式，将“语义相关组件”和“动作相关组件”分离，并以不同时间尺度分别进行优化，从而在保留预训练语义知识的同时高效利用在线交互反馈，为 VLA 模型后训练提供了新思路。

## 2. 方法论
- **核心思想**：语义-动作解耦 + 双时间尺度 RL 后训练。
- **关键技术细节**：
  - **冻结预训练视觉-语言主干**：保留通用语义表示，避免在线 RL 更新破坏大规模预训练得到的视觉-语言知识。
  - **集中适应两个组件**：
    - **语义投影层**：连接语义表示与潜在动作空间的模块，负责保持潜在动作的稳定语义。
    - **低级动作专家**：直接输出控制信号的模块，负责快速响应环境反馈。
  - **双时间尺度更新**：
    - 语义投影层：**低频更新**，以维持潜在动作空间的稳定性。
    - 动作专家：**高频更新**，以快速整合来自在线交互的控制反馈。
  - 通过这种解耦策略，快速策略更新不会传递到语义层，从而避免了语义表示被破坏。
- **算法流程文字说明**：
  1. 初始化：从预训练 VLA 模型加载权重，冻结视觉-语言骨干。
  2. 部署：在环境中执行动作，收集交互数据与奖励。
  3. 分别维护两个 RL 优化循环：一个用于语义投影层（低频率），一个用于动作专家（高频率）。
  4. 动作专家每 K 步更新一次，语义投影层每 M 步更新一次，且 M > K。
  5. 在线迭代直至收敛或达到预期性能。
- 论文未给出具体公式或网络细节，核心贡献在于“按组件功能分配不同 RL 更新节奏”的框架设计。

## 3. 实验设计
- **数据集 / 场景**：
  - **CALVIN 基准**：模拟机器人操作 benchmark，常用于评估 VLA / 机器人策略在语言指令下的长时程操作能力。
  - **真实世界操作任务**：两个真实机器人操作任务，用来验证方法的实际可行性。
- **对比方法**：
  - 预训练的最先进 VLA 模型。
  - 传统 RL 后训练基线（统一更新策略）。
- **评估指标**：评估奖励（evaluation rewards），用于衡量策略在目标任务上的表现质量。
- 论文没有提供更多关于数据集划分、具体任务描述、基线超参等细节。

## 4. 资源与算力
- **文中未明确说明**所用的 GPU 型号、数量、训练时长、模型参数量、内存开销等具体算力信息。
- 仅能从“冻结主干”推测其可能减少部分反向传播计算量，但缺乏数据支撑。
- 整体可复现性受限于未公开的资源与超参细节。

## 5. 实验数量与充分性
- **实验组数**：
  - 一个模拟基准（CALVIN）。
  - 两个真实世界任务。
  - 对比了预训练 SOTA 和 RL 后训练基线。
- **充分性评价**：
  - 优点是包含了模拟和真实场景，结果一致优于基线，具有一定说服力。
  - 不足是实验规模相对有限：缺少消融实验（如不同更新频率比例、是否冻结骨干的影响、解耦 vs 统一更新的细粒度对比）、缺少多个不同 VLA 架构的泛化验证，也缺少对训练曲线稳定性的深入分析。
  - 因此，实验初步证明了方法的有效性，但在系统性和全面性上仍有提升空间。

## 6. 主要结论与发现
- 统一的 RL 后训练策略不可取，因为不同组件功能角色不同，统一更新会破坏语义表示的稳定性。
- 将语义投影层与动作专家解耦，分别采用低频和高频 RL 更新，可以兼顾语义保持与动作学习效率。
- 在 CALVIN 基准和两个真实操作任务上，TEMPO 始终优于预训练 SOTA 模型和 RL 后训练基线。
- 在真实世界任务中，TEMPO 能够达到并维持更高的评估奖励，说明其在实际应用中具有较好的稳定性和收敛性。

## 7. 优点
- **思想清晰**：明确区分 VLA 模型中语义模块与动作模块的角色，提出针对性优化策略。
- **方法简单有效**：不改变模型结构，只改变训练时的更新目标与频率，易嵌入现有 RL 流程。
- **保留预训练知识**：冻结骨干，避免灾难性遗忘，提高训练稳定性。
- **双时间尺度设计合理**：既保证语义稳定，又赋予动作专家快速反应能力。
- **验证范围较广**：同时涉及模拟标准基准与真实世界任务，比纯仿真结果更有说服力。

## 8. 不足与局限
- **实验细节不完整**：缺乏对语义投影层、动作专家的具体网络结构描述，以及 RL 算法（PPO、SAC 等）与超参数设定。
- **消融分析不足**：没有系统研究不同更新频率比例的影响，也未对比“只更新动作专家”或“只更新语义投影层”等变体。
- **对比基线有限**：仅对比统一 RL 后训练，未与现有其他解耦方法或多时间尺度方法比较。
- **泛化性待验证**：只在 CALVIN 和两个真实任务上测试，未覆盖更广泛的 VLA 模型架构（如 RT-2、OpenVLA 等）或多任务场景。
- **算力信息缺失**：没有报告训练成本，难以评估方法在实际部署中的资源需求。
- **潜在偏差**：真实任务可能经过精心挑选，结果可能受到任务难度、环境设置等因素影响，需要更多独立复现来证实。

（完）
