---
title: "Robust-WAM: Bridging Generative Pretraining and Semantic Foresight in World-Action Models"
title_zh: Robust-WAM：在世界-动作模型中桥接生成式预训练与语义预视
authors: "Haodong Yan, Junfeng Li, Junjie He, Zhide Zhong, MingMing Yu, Wenxuan Song, Jiaguan Zhu, Yangyang Zheng, Yuqiao Du, Jiadi You, Yingjie Cai, Xu Yan, Guanyi Zhao, Bingbing Liu, Haoang Li"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05903v1"
tags: ["query:model"]
score: 9.0
evidence: 在世界-动作模型中桥接VAE与语义潜空间，提升动作预测鲁棒性
tldr: 主流世界动作模型在VAE潜在空间利用视频生成预训练，但外观变化下动作预测脆弱，而语义空间虽鲁棒却无法复用该预训练。为此提出Robust-WAM后训练方法，保留VAE生成路径，通过可学习查询令牌将未来真值帧的语义预见对齐到动作流。在分布外模拟基准和真实机器人上，该方法一致提升多个WAM基线的成功率，且不牺牲分布内性能。其意义在于弥合生成预训练与语义鲁棒性之间的鸿沟，为基于视频生成的世界动作模型提供通用增强。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05903v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 943, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05903v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 824, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05903v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1715, \"height\": 666, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.05903v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 862, \"height\": 514, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05903v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 856, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05903v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 865, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05903v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 862, \"height\": 420, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05903v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1721, \"height\": 300, \"label\": \"Table\"}]"
motivation: 现有WAM在VAE空间利用预训练但鲁棒性差，语义空间鲁棒却无法复用大规模预训练，需兼顾二者。
method: 保留VAE生成路径，在动作流上添加轻量对齐目标，用带位置编码的可学习查询令牌对齐未来语义。
result: 在分布外模拟和真实机器人实验中，一致提升多个WAM基线成功率，且不影响分布内性能。
conclusion: 通过语义预见对齐，Robust-WAM兼顾生成预训练与外观鲁棒性，可通用增强基于视频生成的世界动作模型。
---

## 摘要
主流世界-动作模型（WAM）通过改编预训练的视频生成模型（VGM）用于机器人控制，将其学习到的动力学先验迁移到动作预测中。这些VGM通常在变分自编码器（VAE）潜在空间中训练。然而，VAE潜在空间是为像素重建而优化的，这奖励了精细的外观细节，并使动作预测在视觉变化下显得脆弱。近期的工作在语义潜在空间中构建WAM，这类模型对外观变化更具鲁棒性。然而，这些模型无法利用仅存在于VAE空间中的大规模VGM预训练。为克服这一困境，我们提出了Robust-WAM，一种基于视频生成的WAM的通用后训练方法，它保留了基于VAE的生成路径，并在动作流上添加了一个轻量级的语义预视对齐目标。这既保留了大规模VGM预训练，又将动作锚定在与外观无关的动力学中，使其在光照变化和其他视觉分布外条件下保持可靠。具体而言，我们采用可学习的查询标记，通过将其输出隐藏状态与未来真实帧的语义预视对齐，将未来场景语义引入动作流。为了在每个查询与其所描述的未来步骤之间建立时间对应关系，我们为其赋予匹配动作标记的位置编码。在分布外泛化模拟基准和真实机器人平台上的实验表明，我们的Robust-WAM在不牺牲分布内性能的情况下，持续提高了多个WAM基线的成功率。

## Abstract
Mainstream World-Action Models (WAMs) adapt pretrained video generation models (VGMs) for robot control, transferring their learned dynamics prior for action prediction. These VGMs are typically trained in a variational autoencoder (VAE) latent space. However, the VAE latent space is optimized for pixel reconstruction, which rewards fine appearance detail and leaves the action prediction fragile under visual shifts. Recent works build WAMs in semantic latent space, which are more robust to appearance shifts. However, these models cannot leverage the large-scale VGM pretraining that exists only in VAE space. To overcome this dilemma, we propose Robust-WAM, a general post-training method for video-generation-based WAMs that preserves the VAE-based generative path and adds a lightweight semantic foresight alignment objective on the action stream. This retains the large-scale VGM pretraining while grounding actions in appearance-invariant dynamics that stay reliable under illumination shifts and other visual out-of-distribution conditions. Specifically, we employ learnable query tokens to bring future-scene semantics into the action stream by aligning their output hidden states with the semantic foresight of future ground-truth frames. To establish the temporal correspondence between each query and the future step it describes, we give it the positional encoding of the matching action tokens. Experiments on out-of-distribution generalization simulation benchmarks and a real-robot setup show that our Robust-WAM consistently improves the success rates of multiple WAM baselines without sacrificing in-distribution performance.

---

## 论文详细总结（自动生成）

# Robust-WAM 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **问题背景**：世界-动作模型（WAM）通过改编预训练的视频生成模型（VGM）并将动力学先验迁移到动作预测中，用于机器人操控任务。这类VGM通常在VAE潜在空间中训练。
- **核心矛盾**：论文指出WAM面临一个根本性的两难困境：
  - **VAE潜空间WAM**：继承大规模VGM预训练所获得的强动力学先验（如物体运动、接触、交互规律），但VAE潜空间为像素重建优化，迫使空间保留纹理、光照等外观细节，导致动作预测在视觉分布外（OOD）条件下（如光度变化、色调变化）性能脆弱。
  - **语义潜空间WAM**：这类模型（如在DINO、V-JEPA等语义空间构建的WAM）对外观变化鲁棒，但放弃了大规模VGM预训练，重新获取这些知识需要在大规模视频数据上代价高昂的重新预训练。
- **研究意义**：解决这一困境，使WAM既能继承大规模VGM预训练优势，又具备对视觉外观变化的鲁棒性，对于机器人操控在真实世界中的泛化具有重要价值。

## 2. 论文提出的方法论：Robust-WAM

### 2.1 核心思想
- 不替换整个潜在空间，而是**保留VAE生成路径**，在动作流上直接修正外观偏差。
- 通过**可学习查询令牌**注入语义预视（semantic foresight），使动作生成在语义层面获得与外观无关的未来信息。

### 2.2 关键技术细节

**（1）语义预视对齐目标**
- 在动作扩散Transformer（Action DiT）流的噪声动作令牌前**添加K个可学习的查询令牌**（每个未来帧、每个相机视角各一个）。
- 将查询令牌的输出隐藏状态与**冻结的DINOv3 CLS嵌入**（对应未来帧ground truth）对齐。
- 选择DINOv3 CLS嵌入作为目标的原因：自监督语义特征对外观变化大体不敏感，CLS令牌总结场景内容（物体及其空间排列）而丢弃纹理和光照细节。
- 对齐到**未来帧**而非当前帧：使查询编码演示动作引导至何处，为动作流提供外观不变的预视信息。

**（2）共享位置编码（时间对应）**
- 每个查询令牌复用与它描述的**未来步骤所对应的动作令牌的位置编码**，使模型知道该查询所"展望"的未来有多远。
- 帧j对应的动作步长为：$τ_j = min(j∆, L)$（其中$\Delta$为控制步长，L为动作horizon）。

**（3）对齐损失**
- 使用余弦相似度损失：
  $$L_{align} = \frac{1}{K} \sum_{k=1}^{K} [1 - \cos(g(\bar{h}_k), z^*_k)]$$
- 总损失：$L = L_{WAM} + λ_{align} L_{align}$，其中$λ_{align} = 0.1$。

**（4）架构通用性**
- **Action-Expert WAM**（如GE-Act、FastWAM、Motus）：直接插入动作专家（action expert）的令牌序列。
- **Unified WAM**（如LingBot-VA）：插入动作段并分配与动作令牌相同的序列、帧和噪声ID，继承原始注意力模式。

**（5）推理阶段**
- 训练时使用的DINOv3教师和线性对齐头在推理时**被丢弃**，仅保留查询令牌在序列中——不增加任何推理开销。

## 3. 实验设计

### 3.1 仿真基准
- **LIBERO / LIBERO-Plus**：
  - 在4个标准LIBERO套件上训练（约6,000个演示），在干净套件和LIBERO-Plus的全部扰动任务上评估。
  - 扰动轴涵盖**7个方面**：相机视角、光照、背景纹理、物体布局、机器人初始状态、语言和传感器噪声。
- **RoboTwin clean → random**：
  - 在2,500个双机械臂演示（官方干净划分）上训练LingBot-VA基线及其Robust-WAM变体，训练15K步。
  - 评估于干净划分和随机化划分（含未见物体位姿、纹理和光照），每任务100个回合。

### 3.2 真实机器人实验
- **平台**：Franka Research 3，3个桌面任务（Carrot→Bread、Kiwi→Basket、Stack Cups）。
- **协议**：仅标准光照下收集演示，评估时引入紫色和青色两种未见光照条件。每任务每条件25次滚动，共50次OOD滚动。

### 3.3 对比方法
- **VLA方法**：OpenVLA、WorldVLA、NORA、UniVLA、π0、π0-FAST、OpenVLA-OFT、StarVLA、GR00T-N1.7。
- **WAM方法**：
  - VAE潜空间视频生成WAM：FastWAM、GE-Act、LingBot-VA（Robust-WAM施加在它们之上）。
  - 语义潜空间WAM：LDA-1B（无大规模VGM预训练）。

### 3.4 消融实验（LIBERO-Plus上，以FastWAM为骨干）
- **对齐目标变体**：DINOv3 CLS（本文默认）、DINOv3 Patch、DepthAnything3（DA3）、DINOv3 CLS + DA3组合。
- **查询设计变体**：完整时序查询（默认）、去除时序位置编码、用当前帧目标替代未来帧目标。

## 4. 资源与算力

- **未明确说明**：论文正文**没有提供具体的GPU型号、数量或训练资源配置**。
- 仅有部分间接信息：
  - LingBot-VA在RoboTwin上训练15K步。
  - 其余基线沿用各自发布的训练配置，但未给出详细算力信息。
- **结论**：如需可复现性评估，该方面信息披露不足。

## 5. 实验数量与充分性分析

### 5.1 实验数量
- **2个仿真OOD基准**（LIBERO-Plus、RoboTwin clean→random）。
- **3种WAM骨干**（FastWAM、GE-Act、LingBot-VA），覆盖action-expert和unified两种架构。
- **1个真实机器人平台**（3个任务、2种未见光照条件）。
- **消融实验**（2组设计选择、5个变体）。
- **逐轴分析**（7个扰动轴在LIBERO-Plus上分别报告）。

### 5.2 充分性与客观性评估
- **优点**：
  - 覆盖了多种WAM架构，验证了方法的通用性。
  - 与强基线GE-Act（78.0%的LIBERO-Plus成功率）对比仍能提升，证明效果显著。
  - 与语义潜空间WAM（LDA-1B）对比，直接展示"语义增强而非替换"的优势。
  - 消融实验验证了核心设计选择的必要性。
  - 真实实验设计控制变量得当（仅改变光照，保持场景、物体和相机不变）。
- **不足**：
  - 真实机器人任务仅3个，规模偏小。
  - OOD条件仅覆盖光照变化的真实场景，未验证其他OOD轴（如相机位移、背景变化）下的真实表现。
  - 对比的VLA方法主要集中在LIBERO-Plus表上，RoboTwin上对比方法较少。

## 6. 论文的主要结论与发现

- **核心结论**：Robust-WAM作为一种通用后训练方法，成功弥合了VAE潜空间WAM与语义潜空间WAM之间的"生成预训练—语义鲁棒性"鸿沟。
- **性能表现**：
  - LIBERO-Plus上：GE-Act + Robust-WAM达到**80.9%**，为所有对比方法中最佳；FastWAM提升+9.2点；LDA-1B（45.5%）远低于GE-Act+Robust-WAM（80.9%），证明大规模VGM预训练不可替代。
  - RoboTwin上：LingBot-VA + Robust-WAM从29.8%提升至**34.4%**，保持81.2%的干净成功率。
  - 真实实验：GE-Act + Robust-WAM将OOD成功率从57.3%提升至**80.0%**，InD-OOD差距从22.7点降至**2.7点**。
- **消融发现**：
  - DINOv3 CLS作为对齐目标优于Patch特征、深度特征和组合目标。
  - 未来目标（+9.2）远优于当前帧目标（+3.0），时序位置编码可额外贡献3.9个点。
  - 语义目标对几何/视角变化（布局、相机）也有益，不仅限于光度变化。
- **OOD类型覆盖**：增益涵盖相机（+12.4）、光照（+11.3）、背景（+10.3）、传感器噪声（+18.9）、布局（+7.3）等，显示语义目标提升几何和视角鲁棒性。

## 7. 优点

- **概念简洁、即插即用**：作为通用后训练方法，无需重新预训练视频生成模型或重建潜在空间。
- **无需修改原模型**：保留完整VAE tokenization、视频生成目标和视频-动作架构，与原WAM完全兼容。
- **推理零开销**：训练完成后丢弃DINOv3教师与对齐头，推理仅多保留查询令牌。
- **无需语义预训练大规模模型**：教师特征离线一次性提取（使用冻结的DINOv3），训练成本可控。
- **方法架构通用**：同时适用于action-expert（MoT）和unified WAM。
- **实验设计严谨**：
  - 基线对比采用"相同数据、计划、优化设置"的受控比较。
  - 基准报告采用作者发布的原配置而非自行调参，减少主观偏差。
- **对OOD鲁棒性的理解深化**：通过逐轴分析揭示语义对齐不仅帮助光度变化，也改善布局和相机变化，拓展了对语义表示作用的认识。

## 8. 不足与局限

- **实验覆盖局限**：
  - 消融仅在FastWAM上进行，未验证骨干差异对设计选择的影响。
  - 真实机器人任务种类少（仅3个桌面操作），且只变化光照一个条件。
  - 对语义目标的选择只测试了DINOv3和相关变体，未探索其他语义表示（如V-JEPA）的潜力。
- **潜在偏差/风险**：
  - 消融中组合目标（DINOv3 CLS + DA3）表现下降，作者归因于"联合优化两个查询组的复杂性可能阻碍优化"，但缺乏更深入分析。
  - FastWAM的"机器人初始状态"轴上未观测到改进，作者未深究原因。
  - 未见与语义潜空间WAM（如LDA-1B）的直接公平对比分析（由于训练数据差异，作者提及但未做严格对照）。
- **外部有效性**：
  - 所有仿真OOD测试均基于模拟器渲染，无法完全代表真实OOD分布。
  - 语义教师仅指导至未来帧，对长期时序泛化（如序列异常）的稳健性未做测试。
- **算力信息缺失**：未报告训练时间、GPU数量等资源消耗，影响可复现性和工程部署评估。
- **计算开销**：虽推理无额外成本，但训练时增加了DINOv3特征提取和额外的查询令牌注意力计算（尤其多相机多帧时），论文未量化这部分开销。

（完）
