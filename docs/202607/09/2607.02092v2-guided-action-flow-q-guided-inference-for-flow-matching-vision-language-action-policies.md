---
title: "Guided Action Flow: Q-Guided Inference for Flow-Matching Vision-Language-Action Policies"
title_zh: 引导动作流：流匹配视觉-语言-动作策略的Q引导推断
authors: "Liuhaichen Yang, Zhuang Jiang, Chenchao Sheng, Zezhi Tang"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02092v2"
tags: ["query:model"]
score: 9.0
evidence: 流匹配视觉-语言-动作策略，通过学习的评论家进行测试时引导
tldr: "针对流匹配VLA策略推理时缺乏在线引导且重训成本高的问题，本文提出Guided Action Flow推理框架。该框架冻结预训练的SmolVLA策略，利用从真实成功与失败rollouts训练的动作块Q评论家，在反向流采样时通过动作梯度进行引导。在LIBERO操作任务上，单任务评论家将成功率从68%提升至82%，多任务验证成功率从46%提升至56%，留出测试略有提升。该工作证实了Q引导推理的可行性，同时表明评论家泛化与不确定性感知引导仍是核心瓶颈。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02092v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1772, \"height\": 1006, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02092v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 939, \"height\": 230, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 821, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 796, \"height\": 163, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 711, \"height\": 386, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1524, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 792, \"height\": 163, \"label\": \"Table\"}]"
motivation: 流匹配VLA策略推理时缺乏在线引导，且重新训练成本高，因此需要一种无需重训的测试时引导方法。
method: 该框架冻结预训练SmolVLA策略，利用从真实成败rollouts训练的Q评论家，在反向流采样时通过动作梯度进行引导。
result: "在LIBERO任务上，单任务评论家将成功率从68%提升至82%，多任务验证成功率从46%升至56%，留出测试增益适度。"
conclusion: Q引导推理对冻结流匹配VLA策略可行，但评论家泛化与不确定性感知引导仍是瓶颈，需后续研究。
---

## 摘要
流匹配视觉-语言-动作策略通过迭代传输过程生成机器人动作块，为无需重新训练基策略的测试时引导创造了机会。我们在引导动作流中研究这一机会，这是一种推断时框架，保持预训练的SmolVLA策略冻结，并使用学习到的动作块评价器来引导其逆时间流采样器。该评价器基于真实的成功和失败运行数据训练，可以条件于来自冻结SmolVLA语言通路的任务描述特征，并且在采样期间仅通过动作梯度使用。我们在LIBERO操作任务上评估该方法。单任务评价器在一个种子窗口上将成功率从68.0%提高到82.0%，在另一个种子窗口上从82.0%提高到86.0%。多家族任务描述评价器将验证成功率从46.0%提高到56.0%，而锁定的保留测试增益虽为正但较为温和，从65.0%到67.5%。这些结果支持了对冻结流匹配VLA策略进行Q引导推断的可行性，同时表明评价器泛化和不确定性感知引导仍是核心瓶颈。

## Abstract
Flow-matching vision-language-action policies generate robot action chunks through an iterative transport process, creating an opportunity for test-time guidance without retraining the base policy. We study this opportunity in Guided Action Flow, an inference-time framework that keeps a pretrained SmolVLA policy frozen and uses a learned action-chunk critic to guide its reverse-time flow sampler. The critic is trained from real success and failure rollouts, can condition on task-description features from the frozen SmolVLA language pathway, and is used only through action gradients during sampling. We evaluate the approach on LIBERO manipulation tasks. A single-task critic improves success from 68.0% to 82.0% on one seed window and from 82.0% to 86.0% on another. A multi-family task-description critic improves validation success from 46.0% to 56.0%, while the locked held-out test gain is positive but modest, from 65.0% to 67.5%. These results support the feasibility of Q-guided inference for frozen flow-matching VLA policies, while showing that critic generalization and uncertainty-aware guidance remain the central bottlenecks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：预训练的视觉-语言-动作（VLA）策略在任务分布变化、累积误差或多义指令下仍会失败，而全量微调开销大、硬件敏感且难以在少量本地数据下验证。本文研究一种更模块化的适配路径：能否在推理时，仅利用从冻结策略的 rollout 数据中训练的小型 Q 评论家（critic），通过动作梯度引导流匹配 VLA 的采样过程，从而提升闭环成功率。
- **整体含义**：该工作探索了在**不更新基策略**的前提下，通过**测试时 Q 引导**改善机器人操作策略的可能性。这为轻量化、即插即用的策略调优提供了新的范式，尤其适合消费级 GPU 等资源受限的场景。

### 2. 论文提出的方法论
- **核心思想**：将冻结的 SmolVLA 策略视为反向流匹配采样器，在每一个去噪步中，用一个离线训练的 Q 评论家对估计的干净动作块进行评分，计算其动作梯度，并以此修正流速度，从而将生成过程导向高价值动作。
- **关键技术细节**：
  - **反向流匹配约定**：SmolVLA 采用 `x_t = tε + (1-t)a`，`v_t = ε - a`，推断时从 `t=1` 向 `t=0` 积分。估计的干净动作为 `â = x_t - t v_t`。
  - **评论家训练**：从冻结策略的实际 rollout 中构建数据集，目标为稀疏成功回报 `y`。采用多层感知机，输入包括观测特征 `f_o`、动作块 `a` 及任务特征 `e_τ`。任务特征使用 SmolVLA VLM 文本通路池化隐藏状态（任务描述特征），以增强跨任务泛化。
  - **Q 引导更新**：在每个去噪步，计算评论家集成均值 `¯Q(â)`，求动作梯度 `g = ∇_â ¯Q(â)`，经范数裁剪`c`和集成分歧门控 `m` 后，更新速度为 `v_guided^t = v_t - m · clip(g, c) / β`。门控因子 `m = max(m_min, exp(-α σ_Q))` 基于评论家预测的标准差减少不可靠梯度的干扰。
  - **隔离性**：基策略参数完全冻结，梯度仅通过干净动作估计计算，不反传进 VLA。
- **算法流程**：冻结策略、观测特征和任务特征输入 → 从 `t=1` 开始采样 → 每步计算基速度 `v_t` → 估计 `â` → 计算评论家梯度并裁剪/门控 → 修正速度并积分 → 输出最终动作块。

### 3. 实验设计
- **数据集/场景**：使用 LIBERO 仿真操作基准（含 vanilla、LIBERO-Plus 和 LIBERO-PRO 子集），核心评估集中在 vanilla LIBERO 的空间和物体操作任务。
- **基准对比**：对比冻结 SmolVLA 基线策略（无引导）与加入 Q 引导后的成功率。不涉及其他方法，本质上是一种 on-policy 的自我提升实验。
- **对比/评估协议**：
  - 单任务协议：单个 LIBERO 空间任务。
  - 空间任务迁移协议：限定空间任务族内的多任务评论家。
  - 多家族协议：合并空间和物体任务训练评论家，并在验证和留出测试上进行评估。

### 4. 资源与算力
- 论文中**未明确提及**所使用的 GPU 型号、数量或具体训练时长。仅提及项目目标场景是“消费者 GPU 设置”，暗示其对算力要求较低，但未给出实验中实际使用的硬件信息。

### 5. 实验数量与充分性
- **实验组数**：实验规模不大，主要包括：
  - 2 个种子窗口的单任务评测（各 50 episodes）。
  - 1 次空任务迁移验证（60 episodes）。
  - 1 组多家族任务评论家与验证（50 episodes）及留出测试（40 episodes）。
  - 提供了基线锚点、评论家训练数据统计和负面迁移结果。
- **充分性与客观性**：测试采用真实环境闭环 rollout，且 train/validation 按 episode 级分割以避免数据泄露，留出测试在超参锁定后进行，避免了测试集参与调参。评价方法较严格。但总评测预算偏小（特别是留出测试仅 40 episodes），且未进行多种子或置信区间统计，尚不足以支撑广泛鲁棒性声明。此外，LIBERO-Plus 和 LIBERO-PRO 上仅给出基线锚点，未完成 QGF 评测，实验覆盖有待扩展。

### 6. 论文的主要结论与发现
- Q 引导能在**单任务**上显著提升冻结 SmolVLA 的成功率（+14pp），证实了评论家梯度可有效影响闭环行为。
- **多家族任务评论家**结合 VLM 任务描述特征后，验证成功率有明显提升（+10pp），但在**留出测试**上增益较小（+2.5pp），表明评论家泛化是目前的瓶颈。
- 限定于**空间任务族的评论家**未能产生可靠的跨任务迁移，反而出现净退化，说明单纯增加同族任务数量不够。
- 不确定性感知门控可有效抑制有害梯度，但尚不能完全解决 OOD 问题。
- 总体结论：对冻结流匹配 VLA 策略进行 Q 引导推理是**可行**的，但评论家质量、泛化能力和不确定性检测是决定方法能否可靠提升性能的核心挑战。

### 7. 优点
- **即插即用的适配性**：无需微调基策略，仅更换推理采样逻辑，便于集成到现有 VLA 流程。
- **设计清晰、隔离性强**：评论家梯度不反传 VLA，任务特征复用策略自带的语言表示，保持了模块化。
- **客观的评估协议**：使用 episode 级数据分割、锁定测试、真实环境 rollout，并同时报告正面结果和负面结果（如空任务迁移失败），避免了选择性报告。
- **实用的不确定性处理**：集成分歧门控为简单而有效的 OOD 应对手段，降低了对超参的极端敏感性。

### 8. 不足与局限
- **实验规模小**：单任务仅俩种子窗口，留出测试仅 40 episodes，缺乏置信区间或多次重复。
- **评论家泛化弱**：留出测试增益微小，未能展现强大的泛化能力；对任务分布变化的适应力仍不佳。
- **对超参敏感**：引导强度 `β`、裁剪、门控参数等需要基于验证集精细选择，未提供自动化调整策略。
- **仿真与真实世界差距**：所有实验均在 LIBERO 仿真进行，未在真实机器人上验证。
- **场景覆盖有限**：LIBERO-Plus 和 LIBERO-PRO 仅给出基线锚点，未评估 QGF，尤其 PRO 成功率接近零，可能不适用该方法。
- **评论家目标简单**：使用稀疏成功回报可能不足以区分接近成功但失败的动作块，可能限制评论家的精细排序能力。
- **未与其他方法对比**：未与微调、重排等其他适配策略进行横向比较，证据局限于自比较。

（完）
