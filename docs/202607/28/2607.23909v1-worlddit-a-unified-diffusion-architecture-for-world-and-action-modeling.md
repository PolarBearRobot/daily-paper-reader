---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：用于世界与行动建模的统一扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:model"]
score: 9.0
evidence: 统一扩散变换器实现世界与动作建模
tldr: 许多机器人策略依赖大型预训练视觉语言模型（VLM）作为动作主干，训练开销大。WorldDiT提出统一扩散变换器架构，同时生成连续动作块并预测未来相机帧的RGB patch。在四个LIBERO模拟套件中，以sub-billion参数达到与大型模型相当的平均成功率，位于帕累托前沿。为机器人动作与视觉联合建模的缩放研究提供了强基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 现有机器人策略依赖大型预训练VLM，参数多、训练成本高，且缺乏对视觉世界的联合建模。
method: 采用单一扩散变换器，同时输出连续动作块并预测未来帧的归一化RGB patch目标，实现动作生成与视觉世界建模的统一。
result: 在四个LIBERO模拟套件上，WorldDiT以sub-billion参数达到与大型VLM策略相当的平均成功率，位于方法比较的帕累托前沿。
conclusion: 证明了无需大型预训练VLM即可实现高效的动作与视觉世界联合建模，为未来模型扩展研究提供了坚实的基线。
---

## 摘要
许多最近的机器人策略通过使用大型预训练视觉语言模型（VLM）作为动作骨干来追求更强的控制。我们提出了WorldDiT，一种统一的扩散变换器架构，将动作生成与视觉世界建模相结合，无需大型预训练VLM动作骨干即可实现强性能。在训练过程中，单个扩散变换器生成连续的动作块，并从未来相机帧中预测归一化的RGB块目标。在四个LIBERO模拟套件上，WorldDiT位于报告所有四个套件的方法的总模型参数和平均成功率的帕累托前沿。这些结果为未来的扩展研究提供了一个强大的低于十亿参数的基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前许多机器人策略依赖大型预训练视觉语言模型（VLM）作为动作骨干，这种做法虽然带来了强大的感知和语言理解能力，但也使得规模与架构的贡献难以分离。当一个策略包含数十亿参数时，其强大控制性能可能来自动作设计、预训练骨干或两者的结合，难以分析根本原因。
- **核心问题**：是否可以在不使用大型预训练VLM作为动作骨干的情况下，通过一个统一的扩散Transformer架构，同时实现连续动作生成和未来视觉预测，并保持高水平控制性能？
- **整体含义**：WorldDiT旨在探索一种紧凑、高效的替代方案，为未来机器人学习中的缩放研究提供一个低于十亿参数的强基线，同时挑战“必须依赖大型VLM”的普遍认知。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：使用一个共享的DiT（Diffusion Transformer）骨干网络，在训练时同时完成两项任务：生成连续动作块（action chunk）和预测未来相机帧的归一化RGB patch。推理时仅保留动作生成路径，视觉预测头部被移除，不增加推理开销。
- **关键技术细节**：
  - **输入编码**：使用冻结的MAE图像编码器、共享的Perceiver Resampler、冻结的CLIP文本编码器处理视觉和语言输入；可训练的机器人状态编码器处理状态信息。所有编码后的token通过可训练投影映射到骨干网络的隐藏维度。
  - **目标构建**：动作目标为未来7步的连续动作块（7维）；未来RGB patch目标从未来第7帧（时间偏移H=7）的主相机和腕相机图像中提取，每帧图像被划分为16×16的patch，每个patch归一化后保留64个均匀采样的patch，两相机共128个768维的patch向量。
  - **训练损失**：采用flow matching（流匹配）损失。对每个目标（动作或RGB patch）从标准高斯噪声到干净目标的直线路径进行回归，损失函数为L2范数：\( \mathcal{L}_{\text{flow}} = \mathbb{E}_{\boldsymbol{\epsilon}, y, \tau} \| v_\theta(x_\tau, \tau, c) - (y - \boldsymbol{\epsilon}) \|^2_2 \)。总损失为动作损失与RGB patch损失的加权和，权重分别为0.1和0.001。
  - **模型架构**：DiT骨干深度4，隐藏尺寸1024，16个注意力头，4个注册token。上下文观察步数C=3，预测动作块长度H=7，训练窗口总长度N=10步。
- **算法流程**：
  - **训练**：给定10步窗口，前3步作为观测上下文，后7步作为动作目标，第10帧（偏移H=7）的RGB patch作为视觉目标。使用flow matching训练，骨干网络预测动作和RGB patch的流速度。
  - **推理**：从高斯噪声开始，通过20步Euler积分对动作速度场进行数值积分，得到预测动作块。执行前3个动作后，更新观测窗口并重新规划（receding-horizon control）。所有RGB patch相关模块不参与推理。

### 3. 实验设计：数据集、benchmark、对比方法

- **数据集与benchmark**：使用LIBERO操作基准的四个下游套件：`libero_spatial`、`libero_object`、`libero_goal`、`libero_10`（报告为LIBERO Long）。另外使用`libero_90`（大规模多任务数据）进行预训练。每个套件包含多个任务，评估指标为任务成功率及其均值。
- **对比方法**：共对比了24种方法，分为两类：
  - **使用大型预训练VLM作为动作骨干的**：包括ACoT VLA、MMaDA VLA、VLANeXt、VLA Adapter、OpenVLA OFT、π0.5、MemoryVLA、Discrete Diffusion VLA、VLA 0、π0、GR00T N1、UD VLA、π0FAST、CoT VLA、WorldVLA、SpatialVLA、OpenVLA等。
  - **未使用大型预训练VLM的**：包括DreamVLA、FlowVLA、DiT Policy、Octo、Diffusion Policy、Seer（仅报告Long套件）。
- **评估协议**：WorldDiT在四个套件上分别微调，每个套件评估500个episodes（但其中300个episodes用于阶段性检查点选择，因此并非完全独立测试集）。结果报告在表1和图1中，展示了成功率和模型参数量的关系。

### 4. 资源与算力

- **硬件**：所有训练在一个节点上进行，配备8块RTX Pro 6000 GPU。
- **训练过程**：
  - 先在`libero_90`多任务数据上预训练30个epoch，每GPU批次大小40，梯度累积2，学习率1e-4，余弦调度，1个warmup epoch。
  - 然后在每个下游套件上独立微调，有效批次大小512（每GPU批次32，梯度累积2，8块GPU），学习率1e-4，使用bf16混合精度。
- **训练时长**：论文未明确给出具体训练时间（如小时数）。

### 5. 实验数量与充分性

- **实验数量**：论文报告了WorldDiT在四个LIBERO套件上的单一配置评估结果，共4×500 = 2000个episodes。但其中每个套件的前300个episodes用于检查点选择，因此该数据集的一部分被用于模型选择，导致报告的成功率并非完全独立的测试估计。
- **充分性评估**：
  - **充分性较低**：仅测试了一种模型配置（参数、深度、隐藏尺寸等固定），未进行消融实验（例如，没有去掉视觉预测目标、没有改变动作块长度、没有改变观测上下文步数等）。也未在不同随机种子下重复实验来评估方差。
  - **公平性**：对比方法均引用已发表报告中的分数，而非在同一协议下复现，因此存在评估设置差异（如seed、评估episode数等不一致）。作者承认这一点，并指出WorldDiT的分数不是无偏估计。
  - **泛化性**：仅在LIBERO这一模拟基准上评估，未在真实机器人或其他仿真环境上测试。

### 6. 论文的主要结论与发现

- WorldDiT以**399.084M总参数**（含推理所需冻结模块）和**135.107M可训练参数**，在LIBERO四个套件上取得平均成功率94.9%（Spatial 98.0%、Object 97.0%、Goal 92.8%、Long 91.8%）。
- 在报告的24种方法中，WorldDiT位于**帕累托前沿**：所有平均成功率更高的方法（如ACoT VLA 98.5%）均使用更多总参数；而所有参数少于或等于WorldDiT的方法报告的成功率均更低。
- 这表明**无需大型预训练VLM，一个统一的扩散Transformer即可取得有竞争力的控制性能**，为未来扩展研究提供了紧凑的基线。

### 7. 优点：方法与实验设计亮点

- **架构简洁统一**：单一DiT骨干同时处理动作生成和视觉预测，无需额外的VLM或分词器，减少了模型复杂度和依赖。
- **参数高效**：以低于十亿参数（399M）达到与数十亿参数模型相当的性能，显示出极高的参数效率。
- **训练-推理分离设计**：视觉预测目标只在训练时提供额外监督，推理时完全移除，不增加任何延迟。
- **位于帕累托前沿**：在参数-成功率权衡上处于领先地位，为资源受限的机器人平台提供了实用价值。
- **使用flow matching**：训练稳定且适合连续域，统一了动作和视觉目标的生成框架。

### 8. 不足与局限

- **实验覆盖不足**：仅评估了一种配置，缺乏消融实验（如RGB patch预测的重要性、不同损失权重、不同动作块长度等），也缺少多随机种子复现。
- **评估偏差**：报告中包含用于检查点选择的300个episodes，导致成功率不是无偏测试估计，可能高估了真实性能。
- **泛化性有限**：仅在LIBERO模拟环境上评估，未在真实机器人或其他仿真（如RLBench、MimicGen）上验证，也未展示跨任务或零样本泛化能力。
- **对比公平性存疑**：对比方法分数来源于不同报告，评估协议（episode数、种子、检查点选择）可能不完全一致。
- **代码和权重未公开**：论文提到“不完整的公开可用性”，限制了结果的可复现性。
- **未来方向未验证**：论文讨论了可分解为独立专家、适合异构硬件等潜在优点，但未提供任何实验证据。
- **应用限制**：模型设计针对连续动作和RGB patch，可能不适用于离散动作空间或低维观测；同时需要固定窗口大小的历史观测和语言指令，对实时性要求高的场景可能需要优化。

（完）
